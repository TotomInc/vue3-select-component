---
title: Installation
description: Install Vue 3 Select Component in your project
navigation:
  icon: i-lucide-download
---

## Prerequisites

[Vue.js](https://vuejs.org) **3.5+** is required.

## Install the package

::code-group
```bash [pnpm]
pnpm add vue3-select-component
```

```bash [npm]
npm install vue3-select-component
```

```bash [yarn]
yarn add vue3-select-component
```

```bash [bun]
bun add vue3-select-component
```
::

## Import paths

v1 exposes two entry points:

| Import | Use case |
|--------|----------|
| `vue3-select-component` | Assembled `Select` component (default export) |
| `vue3-select-component/primitives` | Low-level primitive components |

```ts
// Batteries-included
import Select from "vue3-select-component"

// Headless composition
import {
  SelectRoot,
  SelectTrigger,
  SelectValue,
  SelectPopover,
  SelectListbox,
  SelectOption,
} from "vue3-select-component/primitives"
```

## Styling in v1

Unlike v0, v1 primitives ship **unstyled**. Add your own CSS targeting `data-*` hooks (see [Styling](/guide/styling)).

The assembled `Select` uses the same primitives. Style it the same way.
